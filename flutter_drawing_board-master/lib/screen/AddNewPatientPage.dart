import 'package:flutter/material.dart';

class AddNewPatientPage extends StatelessWidget {
 @override
 Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add New Patient'),
      ),
      body: Center(
        child: Text('Page for adding a new patient'),
      ),
    );
 }
}
