import 'package:flutter/material.dart';

class ListExercisesPage extends StatelessWidget {
 @override
 Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('List Exercises'),
      ),
      body: Center(
        child: Text('Page for listing exercises'),
      ),
    );
 }
}
